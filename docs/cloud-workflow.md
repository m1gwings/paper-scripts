# Local and cloud paper workflow

The complete architecture, commands, invariants, setup, Discord notifications, and
recovery guide now live in the [self-contained README](../README.md). Keep that
file as the single reference when sharing context with another chat.
